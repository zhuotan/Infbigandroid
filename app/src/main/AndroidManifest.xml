<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rednum.com.infbigand">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ContextApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".TestJNIActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProjectTopoActivity"></activity>
        <activity android:name=".CompanyBaseInfoActivity" />
        <activity android:name=".CompeteAnalysisResultActivity" />
        <activity android:name=".CompanyInfoShowActivity" />
        <activity android:name=".CompanyZizhiInfoActivity" />
        <activity android:name=".CreditLevelActivity" />
    </application>

</manifest>